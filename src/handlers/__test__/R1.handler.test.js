const { R1getHandler } = require('../R1.handler.js');
const services = require('../../services/R1.service.js');

describe('R1getHandler', () => {
  it('should pass the test', () => {
    const spyOnR1getService = jest.spyOn(services, 'R1getService');
    //TODO
  });
});
